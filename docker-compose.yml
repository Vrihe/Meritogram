version: '3.8'

services:
  backend:
    build: ./Backend
    ports:
      - "8000:8000"
    env_file:
      - ./Backend/.env
    # Пробрасываем переменную из твоего .env в контейнер
    environment:
      - MONGO_URL=${MONGO_URL}